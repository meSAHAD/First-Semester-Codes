
#include <stdio.h>
int main()
{
    char str1[50], str2[50];
    scanf("%s", str1);
    scanf("%s", str2);

    int i = 0;
    while(str1[i] == str2[i] and str1[i] != '\0' and str2[i] != '\0'){
        i++;
    }

    if(str1[i] == '\0' and str2[i] == '\0'){
        printf("Strings are equal\n");
    }
    else{
        printf("Strings are equal\n");
    }

    return 0;
}
